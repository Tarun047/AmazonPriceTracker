# A very simple go lang based cli tool to track product prices at amazon
# Usage
Clone this repo and cd to the root of it
Make sure to have go lang installed
run go build .
run AmazonPriceTracker <product_url>